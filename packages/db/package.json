{
  "name": "db",
  "version": "1.0.0",
  "description": "- running postgresql from docker steps:     - Creating the Docker Postgres instance         docker run -d -e POSTGRES_DB=mydb -e POSTGRES_PASSWORD=testpass123 -e POSTGRES_USER=postgres -p \"6500:5432\" postgres",
  "main": "index.js",
  "scripts": {
    "db:deploy": "npx prisma migrate deploy && npx prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/pg": "^8.16.0",
    "prisma": "^7.2.0"
  },
  "dependencies": {
    "@repo/typescript-config":"workspace:*",
    "@prisma/adapter-pg": "^7.2.0",
    "@prisma/client": "^7.2.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "pg": "^8.16.3"
  }
}
